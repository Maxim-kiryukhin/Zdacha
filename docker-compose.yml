version: '3.8'

services:
  backend:
    build: ./backend
    volumes:
      - ./backend/db.sqlite3:/app/db.sqlite3 # Сохраняем БД на сервере
      - static_volume:/app/static
    environment:
      - DEBUG=0
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  static_volume: